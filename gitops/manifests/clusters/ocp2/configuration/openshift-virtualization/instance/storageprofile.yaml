apiVersion: cdi.kubevirt.io/v1beta1
kind: StorageProfile
metadata:
  name: ocs-storagecluster-ceph-rbd-virtualization-hub
spec:
  claimPropertySets:
    - accessModes:
        - ReadWriteMany
      volumeMode: Block