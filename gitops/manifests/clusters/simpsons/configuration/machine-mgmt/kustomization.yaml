apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonAnnotations:
  argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true

resources:
  - bart-bmh.yaml
  - bart-network-config-secret-sealed.yaml
  - bart-bmc-secret-sealed.yaml
  - worker-kubeletconfig.yaml
  - master-kubeletconfig.yaml
