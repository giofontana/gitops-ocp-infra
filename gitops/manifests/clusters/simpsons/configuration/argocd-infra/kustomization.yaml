apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd-workloads

resources:
  - patch-subscription-job.yaml
  - consolelink.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: patch-subscription-job
    namespace: argocd-workloads
    files:
      - patch-subscription-job.sh
